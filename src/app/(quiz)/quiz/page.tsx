import { InitialForm } from '@/app/(quiz)/quiz/components/initialForm';

export default function Quiz() {
    return (
        <div className='flex justify-around h-full items-center relative'>
            <InitialForm />
        </div>
    );
}
