import { InitialForm } from '@/app/(quiz)/quiz/components/initialForm';

export default function Quiz() {
    return (
        <div className='flex lg:flex-row flex-col justify-around h-full items-center relative w-full'>
            <InitialForm />
        </div>
    );
}
